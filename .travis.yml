language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
jobs:
  include:
    - stage: home
      script:
        - bash ./buildapps/base.sh
        - bash ./buildapps/test.sh
        - bash ./buildapps/covid19.sh

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: build
  on:
    branch: master
