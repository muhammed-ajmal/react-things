language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
jobs:
  include:
    - stage: home
      react_env: $react_env
      script:
        - bash ./buildapps/env.sh
        - bash ./buildapps/base.sh
        - bash ./buildapps/test.sh
        - bash ./buildapps/covid19.sh
        - bash ./buildapps/githubcards.sh

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  react_env: $react_env
  local_dir: build
  on:
    branch: master
